# Cloudflare credentials for DNS challenge
CF_API_EMAIL=your-cloudflare-email@example.com
CF_DNS_API_TOKEN=your-cloudflare-dns-api-token

# ACME (Let's Encrypt) email for certificate notifications
ACME_EMAIL=your-email@example.com

# Base domain for Traefik dashboard (dashboard will be at traefik.DOMAIN)
DOMAIN=mikeng.io

# Traefik dashboard basic auth (generate with: htpasswd -nB admin)
# Example: admin:$2y$05$...
TRAEFIK_DASHBOARD_AUTH=admin:$$2y$$05$$hashedpassword
